# =========================
# SERVER
# =========================
server.port=8080

# =========================
# POSTGRESQL (RENDER DB)
# =========================
spring.datasource.url=jdbc:postgresql://dpg-d7rjld28qa3s73diad3g-a.oregon-postgres.render.com/ems_db_qkjd?sslmode=require
spring.datasource.username=ems_db_qkjd_user
spring.datasource.password=XCJCxwKLOEXSsQKfUGv83TEoMfHULXPq
spring.datasource.driver-class-name=org.postgresql.Driver

# =========================
# HIKARI CONNECTION POOL (FIX FOR YOUR ERROR)
# =========================
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=10000
spring.datasource.hikari.max-lifetime=30000
spring.datasource.hikari.keepalive-time=15000
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.validation-timeout=5000
spring.datasource.hikari.leak-detection-threshold=20000

# =========================
# JPA / HIBERNATE
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Prevent some lazy connection issues
spring.jpa.open-in-view=false

# =========================
# CORS
# =========================
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*

# =========================
# JWT (optional)
# =========================
# jwt.secret=your-secret-key